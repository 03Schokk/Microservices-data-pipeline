```plantuml

@startuml C4_SystemContext
' Подключаем официальную библиотеку C4
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Container.puml

LAYOUT_WITH_LEGEND()

title Диаграмма контекста системы (Level 1) – Система аналитических отчётов

Person(admin, "Администратор", "Пользователь, формирующий отчёты по лабораторным работам")

System(report_system, "Система отчётов по лабораторным работам", "Предоставляет API‑методы для генерации тестовых данных\nи выполнения аналитических запросов с JWT-аутентификацией")

Rel(admin, report_system, "Получает отчёты через HTTP(S), используя JWT токен", "HTTPS")
Rel(report_system, admin, "Возвращает JSON-отчёт", "JSON/HTTPS")

@enduml
```